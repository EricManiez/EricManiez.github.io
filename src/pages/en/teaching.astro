---
import BaseLayout from '../../layouts/BaseLayout.astro';
import TeachingShowcase from '../../components/TeachingShowcase.astro';
import { t } from '../../i18n/strings';

const locale = 'en';
---

<BaseLayout
  title={`${t(locale, 'teaching.title')} — Eric Maniez`}
  description={t(locale, 'teaching.subtitle')}
  wide
>
  <TeachingShowcase locale={locale} />
</BaseLayout>
