---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import MusicShowcase from '../../../components/MusicShowcase.astro';
import { t } from '../../../i18n/strings';

const locale = 'fr';
---

<BaseLayout
  title={`${t(locale, 'music.title')} — Eric Maniez`}
  description={t(locale, 'music.subtitle')}
  wide
>
  <MusicShowcase locale={locale} />
</BaseLayout>
